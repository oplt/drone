/**
 * Color mode control for ops + auth.
 * Prefer the icon toggle everywhere so auth and console share one metaphor.
 * ColorModeSelect remains for rare cases that need explicit system/light/dark.
 */
export { default } from "./ColorModeIconDropdown";
