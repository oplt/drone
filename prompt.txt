You are a senior staff-level AI engineer, drone software architect, frontend performance engineer, backend engineer, and product UX reviewer.

The Agriculture workflow and Video Analysis / Agriculture Vision functionality have recently undergone significant development in this repository.

Your task is **NOT to immediately implement more features**.

Your task is to conduct a deep audit of the CURRENT WORKING TREE and produce a highly actionable implementation roadmap in:

`tasks.txt`

The audit must cover:

1. Agriculture workflow/product flow
2. Agriculture frontend UX
3. Agriculture backend architecture
4. Recently implemented Vision Models workflow
5. Dataset creation/curation
6. Image annotation/labeling
7. Model training
8. Evaluation
9. Model registry/deployment
10. Video analysis
11. YOLO inference
12. SAHI/small-object inference if present
13. Tracking if present
14. Telemetry/geolocation integration
15. Performance
16. Database/API efficiency
17. Frontend performance
18. Bugs and broken flows
19. Error handling
20. Duplicate code/features
21. Dead/unnecessary code
22. Unnecessary dependencies
23. UX complexity
24. Product gaps compared with leading agricultural-drone software

The final deliverable is a detailed `tasks.txt` roadmap that another Codex session can implement task-by-task.

Do not make broad production-code changes during this audit unless a tiny change is absolutely necessary to run or inspect the system.

---

# 1. Read repository instructions first

Before doing anything else, read:

```text
AGENTS.md
README.md
DESIGN.md
PRODUCT.md
```

and any relevant README/API contract/operations files under:

```text
backend/modules/agriculture/
backend/modules/video_analysis/
backend/modules/vision_models/
```

or whatever equivalents currently exist.

The repository may have changed significantly since earlier work.

The CURRENT WORKING TREE is the source of truth.

---

# 2. Inspect Git state first

Run:

```bash
git status
git diff --stat
git diff
git log --oneline -20
```

Determine:

* committed changes;
* uncommitted changes;
* recently created files;
* recently modified Agriculture files;
* recently modified Video Analysis files;
* recently added Vision Model files.

Do NOT assume GitHub `main` represents the current implementation.

Do NOT discard or overwrite existing work.

---

# 3. Build a current architecture map

Before recommending changes, map the actual system.

Identify the complete path for:

```text
Agriculture UI
      ↓
Frontend API/hooks
      ↓
FastAPI endpoint
      ↓
Application/service
      ↓
Repository
      ↓
Database
```

and separately:

```text
Drone mission/video
      ↓
Video asset
      ↓
Frame extraction
      ↓
Detector
      ↓
Optional SAHI
      ↓
Optional tracking
      ↓
Telemetry matching
      ↓
Detection persistence
      ↓
Analysis summary
      ↓
Frontend result visualization
```

and:

```text
Training images/video
      ↓
Dataset
      ↓
Frame curation
      ↓
Annotation
      ↓
Dataset split
      ↓
YOLO training
      ↓
Evaluation
      ↓
Model version
      ↓
Deployment
      ↓
Video Analysis model selector
```

Document actual classes/functions/modules involved.

Use real file paths.

---

# 4. Trace the Agriculture user journey

Analyze the application from the perspective of a farmer/agronomist rather than a developer.

Trace every step from:

```text
Open Agriculture
```

through the available workflows.

Determine what the current Agriculture experience actually contains.

Examples might include:

```text
Fields
Missions
Crop monitoring
Field survey
Vegetation analysis
Alerts
Vision models
Video analysis
Results
Maps
Reports
```

Do not assume these names exist.

Inspect the actual current UI.

For every Agriculture screen determine:

```text
purpose
inputs
outputs
next expected user action
navigation
dependencies
API calls
loading behavior
empty state
error state
```

Identify where users can become confused or reach a dead end.

---

# 5. Identify the intended primary workflow

Determine whether the current Agriculture module has a clear main workflow.

A good product flow might conceptually resemble:

```text
Field
  ↓
Mission
  ↓
Collect imagery/video
  ↓
Process
  ↓
Analyze
  ↓
Review findings
  ↓
Map issues
  ↓
Take action
```

and for custom AI:

```text
Vision Models
  ↓
Create crop-specific project
  ↓
Build dataset
  ↓
Label
  ↓
Train
  ↓
Evaluate
  ↓
Deploy
  ↓
Analyze mission
```

Compare these conceptual flows with the actual application.

Identify unnecessary detours.

Identify screens that expose internal implementation concepts unnecessarily.

---

# 6. Research leading agriculture-drone products

Use current web research.

Do NOT rely only on model memory.

Search current products, websites, documentation, screenshots, tutorials, videos where useful, and official product pages.

Start with, but do not limit research to:

```text
DJI Agriculture / SmartFarm
DroneDeploy Agriculture
PIX4Dfields
Sentera / FieldAgent
Agremo
Solvi
Delair
OneSoil
Mapware agriculture capabilities
Agisoft agriculture workflows where relevant
```

Verify which products are currently active and relevant before using them.

If stronger/current competitors exist, include them.

Aim to deeply inspect approximately:

```text
5–8 strong current products
```

rather than superficially listing 20.

---

# 7. Competitor research questions

For each relevant product investigate:

### Mission workflow

How does the user go from:

```text
field
→ flight
→ imagery
→ analysis
```

?

### Analysis selection

How are analysis types presented?

Examples:

```text
plant count
stand count
weed detection
disease/stress
NDVI
NDRE
crop health
canopy
yield
damage
missing plants
```

### Results

How are results presented?

Examples:

```text
map
heatmap
zones
detections
statistics
alerts
reports
prescriptions
```

### AI/model workflow

Do they expose:

```text
model names?
confidence thresholds?
technical settings?
```

or abstract those details?

### Upload workflow

How do users:

```text
upload imagery
select mission
select field
select analysis
```

?

### Processing status

How do they represent:

```text
queued
processing
completed
failed
```

?

### Actionability

What does the user do AFTER analysis?

This is especially important.

Does the product provide:

```text
field zones
issue locations
export
report
prescription map
comparison
historical trend
task creation
re-flight
```

?

---

# 8. Create a competitor feature matrix

During analysis create a matrix similar to:

```text
Feature                     Drone App   DJI   PIX4D   DroneDeploy   Sentera   Agremo
-------------------------------------------------------------------------------------
Field management
Mission planning
Flight execution
Image upload
Video upload
Orthomosaic
Vegetation indices
Plant counting
Weed detection
Disease/stress
Custom AI models
Annotation
Model training
Model evaluation
Object tracking
Geolocated detections
Time-series comparison
Reports
Export
Prescription maps
```

Do not blindly add missing competitor features to the roadmap.

Use the comparison to identify features that materially improve THIS application's intended value.

---

# 9. Separate product gaps from technical gaps

Classify findings.

### Product gap

Example:

```text
Analysis produces detections but gives the user no agricultural interpretation.
```

### UX gap

Example:

```text
User must understand YOLO model filenames to run analysis.
```

### Architecture gap

Example:

```text
Agriculture-specific logic is coupled directly into the generic video pipeline.
```

### Performance gap

Example:

```text
Frontend downloads all full-resolution evidence images eagerly.
```

### Reliability bug

Example:

```text
Failed Celery job can leave video permanently marked analyzing.
```

### Data-quality problem

Example:

```text
Adjacent video frames leak into train and validation splits.
```

Do not mix these categories.

---

# 10. Audit the Agriculture frontend deeply

Inspect:

```text
frontend/src/modules/agriculture/
```

and related modules used by Agriculture.

Check for:

```text
very large files
very large hooks
very large API modules
duplicated request logic
duplicated types
duplicated UI components
duplicate forms
duplicate state
unnecessary useEffect
manual polling
prop drilling
large context objects
unnecessary rerenders
huge components
poor query-key design
over-fetching
N+1 frontend requests
waterfall requests
unnecessary refetches
lack of pagination
lack of virtualization where actually needed
full-resolution image loading
incorrect memoization
stale state
race conditions
```

Pay particular attention to any files that have become several hundred or thousand lines.

Large file size alone is not automatically a problem.

Determine whether responsibilities are genuinely mixed.

---

# 11. Audit React Query usage

Inspect query keys and mutation flows.

Look for:

```text
duplicate fetches
overly broad invalidation
invalidating entire Agriculture domain
refetch after every small mutation
manual useEffect fetching
manual setInterval polling
queries depending on unstable objects
queries firing before required IDs exist
poor staleTime choices
missing pagination
```

Recommend improvements where measurable.

Do not recommend memoization/caching without a concrete reason.

---

# 12. Audit the annotation workspace

Inspect the recently created labeling implementation.

Check:

```text
canvas performance
coordinate conversion correctness
zoom
pan
fit-to-window
box drawing
resize
drag
class change
deletion
autosave
optimistic mutation behavior
query invalidation
image prefetching
thumbnail loading
keyboard shortcuts
review state
negative images
unsaved navigation
```

Look specifically for coordinate bugs caused by:

```text
original-image dimensions
canvas dimensions
CSS dimensions
zoom
pan
devicePixelRatio
```

Test round-trip transformations.

---

# 13. Audit the dataset workflow

Inspect:

```text
image upload
video import
frame extraction
quality filtering
near-duplicate filtering
dataset versioning
train/val/test splitting
annotation completeness
training readiness
```

Find bugs such as:

```text
duplicate images
race conditions
lost metadata
incorrect source references
dataset mutability after training
bad split leakage
poor handling of empty/negative images
```

Evaluate whether frame-selection algorithms are efficient enough for long drone videos.

---

# 14. Audit training

Inspect the actual YOLO training implementation.

Check:

```text
Celery isolation
GPU/CPU selection
training lifecycle
callbacks
progress reporting
artifact paths
best checkpoint handling
failure handling
cleanup
cancellation
worker blocking
memory handling
dataset YAML generation
dataset checksum
model version linkage
```

Determine whether repeated training loads unnecessary resources.

Check whether GPU workers should be separated from ordinary Celery workers.

Only recommend this if the current architecture genuinely benefits.

---

# 15. Audit evaluation

Inspect:

```text
metrics extraction
metrics persistence
per-class metrics
confusion matrix
PR/F1 curves
hard-example handling
evaluation state
frontend presentation
```

Ensure values shown to the user are actual evaluator outputs.

Check for confusing use of terms such as:

```text
accuracy
```

where mAP/precision/recall are intended.

---

# 16. Audit model registry

Inspect:

```text
VisionModel
ModelVersion
deployment
candidate
production
archived
```

Determine whether:

```text
model
version
training run
dataset
weights
evaluation
```

have clean ownership relationships.

Identify unnecessary duplication of metadata.

Check if filesystem paths are leaking into API contracts.

They should not.

---

# 17. Audit the Video Analysis frontend

Trace the entire UI flow.

Example:

```text
select recording
select/upload video
select model
configure
run
wait
results
```

Determine whether this workflow is intuitive.

Look for duplicated concepts between:

```text
Agriculture
Vision Models
Video Analysis
Mission History
Field Survey
```

For example:

If a mission already contains a recording, the user should not have to manually upload the same file again.

Identify these kinds of workflow redundancies.

---

# 18. Audit Video Analysis backend

Inspect the complete current pipeline.

Pay attention to:

```text
frame extraction
frame decoding
stride
inference
SAHI
tracking
telemetry matching
crop/evidence saving
batch persistence
progress reporting
metrics
error handling
model loading
model caching
job state
```

Look for functional bugs and performance bottlenecks.

---

# 19. Profile the video pipeline

Do not optimize purely from intuition.

Where possible, measure or instrument a representative run.

Break elapsed processing time into:

```text
video decoding
frame extraction
model loading
inference
SAHI slicing
tracking
crop saving
telemetry matching
database writes
serialization
```

If no sample video exists, inspect existing test/demo assets or create a small synthetic benchmark that does not get committed unnecessarily.

Identify the dominant bottleneck.

---

# 20. Check GPU execution correctness

Verify whether inference actually runs on GPU when CUDA is available.

Inspect:

```text
device selection
PyTorch CUDA availability
model placement
worker environment
Docker GPU configuration
```

Look for accidental CPU paths.

Also inspect whether:

```text
run_blocking
thread pools
Celery concurrency
```

interact poorly with CUDA inference.

Recommend changes only with technical justification.

---

# 21. Model caching audit

Inspect current model cache behavior.

Determine:

```text
how many models can be cached
whether model versions invalidate correctly
whether stale weights can remain cached
whether multiple workers duplicate GPU memory
whether training/inference models interfere
```

Identify safe improvements.

Do not introduce complex model-serving infrastructure unless justified.

---

# 22. Video decoding efficiency

Check whether the pipeline is doing something equivalent to:

```text
decode every frame
discard most frames
```

when a large stride is requested.

Investigate whether seeking or efficient frame sampling could materially help.

Consider:

```text
OpenCV
PyAV
FFmpeg
```

based on current repository dependencies and architecture.

Do not automatically introduce a new media stack.

Quantify likely benefit first.

---

# 23. SAHI audit

If SAHI is implemented, inspect:

```text
tile size
overlap
model reuse
batching
postprocessing
coordinate conversion
NMS
memory
```

Determine whether each tile is processed independently in a way that causes avoidable overhead.

Look for opportunities such as:

```text
batched tile inference
larger inference batch
conditional use based on resolution/object scale
```

but confirm library capabilities before recommending.

---

# 24. Tracking audit

Inspect:

```text
tracker type
tracker lifecycle
job isolation
frame stride
camera motion
class handling
track IDs
summary calculation
```

Look for:

```text
state leakage between jobs
incorrect tracker reset
unstable IDs
tracking across excessively sparse frames
duplicate counts
```

Verify whether unique-object counts are described as estimates.

---

# 25. Telemetry/geospatial audit

Verify that detections correctly preserve:

```text
timestamp
latitude
longitude
altitude
heading
```

where available.

Check:

```text
timestamp alignment
mission/video start-time assumptions
nearest telemetry matching
large time gaps
missing telemetry
```

Determine whether geolocation quality/confidence is surfaced appropriately.

---

# 26. Database performance audit

Inspect relevant queries.

Look for:

```text
N+1 queries
missing indexes
large JSON blobs
unbounded result sets
large detection fetches
unnecessary ORM object loading
frequent commits
one-row-at-a-time inserts
poor pagination
```

Pay particular attention to video detections.

A single video can generate many thousands of rows.

Determine whether APIs paginate results.

---

# 27. Batch-write performance

Inspect how detections are persisted.

Measure/reason about:

```text
batch size
flush frequency
transaction boundaries
ORM overhead
```

If current code constructs thousands of ORM entities individually, determine whether SQLAlchemy bulk insert approaches would materially improve performance without damaging maintainability.

Do not optimize without evidence.

---

# 28. Evidence-image storage audit

Inspect detection crop creation.

Determine:

```text
how many crops are created
whether every detection needs one
JPEG quality
filesystem overhead
storage growth
duplicate crops
```

Estimate storage implications.

For example:

```text
100,000 detections
× crop size
```

Identify whether evidence should be:

```text
all detections
only representative detections
only highest-confidence detections
configurable
```

Make a recommendation.

---

# 29. API payload audit

Inspect responses that return:

```text
detections
dataset images
annotations
training runs
model metrics
```

Identify unnecessarily large payloads.

Recommend:

```text
pagination
summary endpoints
server-side aggregation
lazy evidence loading
```

where appropriate.

---

# 30. Frontend result rendering audit

Large video jobs may contain thousands of detections.

Check whether the frontend tries to render all of them simultaneously.

Inspect:

```text
tables
maps
charts
image lists
```

Look for performance issues.

Possible solutions include:

```text
pagination
aggregation
server-side filtering
viewport map clustering
virtualization
sampling
```

Choose based on actual implementation.

---

# 31. Memory-leak / lifecycle audit

Inspect frontend for:

```text
unreleased object URLs
video elements
canvas listeners
map listeners
intervals
timeouts
subscriptions
large cached arrays
```

Inspect backend for:

```text
model references
large numpy frames
large result objects
CUDA tensors
temporary directories
```

Confirm resources are released appropriately.

---

# 32. Error/retry audit

Map failures for:

```text
video upload
frame extraction
model load
inference
SAHI
tracking
database write
training
evaluation
annotation save
```

Determine whether users receive actionable errors.

Look for invalid states such as:

```text
job stuck running forever
video stuck analyzing
training stuck queued
```

Recommend idempotent retry behavior where appropriate.

---

# 33. Concurrency audit

Investigate:

```text
multiple video jobs
multiple training jobs
GPU contention
Celery worker concurrency
database pool pressure
filesystem collisions
model cache concurrency
```

Look specifically for thread safety around:

```text
YOLO models
trackers
CUDA
```

Do not assume model inference objects are safely shareable.

---

# 34. Security/tenancy audit

Verify organization boundaries across:

```text
fields
missions
videos
datasets
annotations
training runs
models
model versions
detections
artifacts
```

Check for direct-ID object access without ownership validation.

Flag any cross-organization data risk as P0.

---

# 35. Duplication audit

This is a major requirement.

Search systematically for duplicate functionality.

Use:

```bash
rg
```

and repository inspection.

Look for duplicate concepts such as:

```text
video upload
video metadata
model selectors
YOLO loading
frame extraction
mission selection
field selection
telemetry matching
file storage
API client wrappers
status badges
polling hooks
date formatting
error handling
pagination
crop type definitions
```

Determine which implementation should be canonical.

---

# 36. Duplicate frontend types

Look for equivalent interfaces defined separately across:

```text
agriculture/types.ts
video-analysis/types.ts
vision-models/types.ts
mission types
```

Do not merge types only because names resemble each other.

Merge only genuinely identical domain contracts.

Flag:

```text
same concept with different incompatible definitions
```

as technical debt.

---

# 37. Duplicate backend models/services

Search for functionality duplicated between:

```text
agriculture
video_analysis
vision_models
field_survey
mission_history
```

Examples:

```text
VideoAsset
frame extraction
model resolver
telemetry matcher
artifact storage
analysis job state
```

Recommend consolidation only when it reduces complexity without creating an oversized "god module."

---

# 38. Dead-code audit

Identify code that is genuinely unused.

Use evidence such as:

```text
no imports
no routes
no references
obsolete compatibility paths
replaced implementation
unreachable UI
unused API
deprecated models
old components
```

Do NOT label code dead merely because a text search fails if framework registration/dynamic import may use it.

Be conservative.

---

# 39. Dependency audit

Inspect:

```text
backend/requirements.txt
frontend/package.json
```

Identify dependencies that:

```text
are no longer imported
duplicate another library
were added experimentally
are unnecessarily heavyweight
```

Do not recommend removing something without checking actual usage.

For every dependency proposed for removal state:

```text
dependency
search evidence
replacement if any
risk
```

---

# 40. Find oversized files

Identify the largest relevant frontend/backend source files.

For example use:

```bash
find backend/modules/agriculture backend/modules/video_analysis backend/modules/vision_models \
  -type f -name '*.py' -print0 | xargs -0 wc -l | sort -nr | head -30
```

and equivalent for frontend TypeScript.

Determine whether each large file has:

```text
one coherent responsibility
```

or contains multiple unrelated responsibilities.

Recommend splitting only the latter.

---

# 41. Find TODO/FIXME/workarounds

Search:

```bash
rg -n "TODO|FIXME|HACK|XXX|temporary|workaround|deprecated|legacy|compat" \
  backend frontend
```

Review all relevant findings.

Do not automatically convert every TODO into a roadmap task.

Determine whether each is still relevant.

---

# 42. Bug-hunting strategy

Run existing tests first.

Then inspect failing or suspicious flows.

Use:

```text
unit tests
integration tests
frontend tests
Playwright
type checking
lint
build
```

as appropriate.

Also inspect code for classes of subtle bugs:

```text
wrong async usage
race conditions
stale closures
incorrect query invalidation
timezone mismatches
coordinate scaling
transaction rollback
job retries
orphan artifacts
partial database writes
incorrect status transitions
```

---

# 43. Run the frontend where practical

If environment permits:

start the application or frontend against its development/mock environment.

Inspect the Agriculture experience interactively.

Do not limit UX review to source code.

Walk through:

```text
Agriculture
Vision Models
Dataset
Label
Train
Evaluation
Video Analysis
Results
```

Capture concrete UX issues.

If application startup is blocked by infrastructure, document why and continue with code/test analysis.

---

# 44. Performance measurement

Measure before recommending major optimization.

Where possible collect:

```text
frontend production bundle size
Agriculture route chunk size
Vision labeling chunk size
API request count
query waterfalls
video-analysis processing timing
database query counts
```

Use existing observability/profile tools where available.

Do not add permanent profiling dependencies during this audit.

---

# 45. Agriculture domain quality

Evaluate whether the current video-analysis results are agriculturally useful.

Detection output alone is usually not enough.

Check whether outputs become useful domain information such as:

```text
plant count
fruit count estimate
weed hotspots
damaged crop locations
crop row issues
missing plants
disease/stress areas
density
field zones
map overlays
historical comparison
```

Only recommend analyses appropriate to available RGB/video/sensor data.

Do not promise multispectral analysis from RGB footage.

---

# 46. Actionability audit

For each analysis ask:

> What can the farmer do with this result?

Weak:

```text
1,823 detections
```

Better:

```text
Estimated 824 unique tomato fruits
72 damaged
5 clusters with high damage density
```

Better still:

```text
5 damage hotspots shown on map
[Inspect hotspot]
[Export findings]
[Create follow-up mission]
```

Recommend improvements that move the application toward actionable decisions.

---

# 47. Avoid feature overload

Do NOT recommend implementing every competitor feature.

The project should remain coherent.

Classify potential features:

```text
Core
Useful
Later
Not relevant
```

Prefer features that reuse existing drone/video/geospatial infrastructure.

---

# 48. Simplify technical UX

Identify technical settings unnecessarily exposed to agriculture users.

Examples could include:

```text
raw YOLO filename
SAHI
IoU threshold
tracker implementation
storage paths
batch size
image size
```

Recommend user-centered language such as:

```text
Analysis model
Minimum confidence
Small-object mode
Tracking
Training quality
```

Keep advanced settings behind an advanced section if needed.

---

# 49. Analyze navigation

Determine whether:

```text
Agriculture
Field Survey
Vision Models
Video Analysis
Mission History
```

are arranged coherently.

Look for a user having to mentally understand the code architecture to navigate the product.

Recommend a workflow-oriented hierarchy instead.

For example, only if justified:

```text
Agriculture
 ├── Fields
 ├── Missions
 ├── Analyze
 ├── Findings
 └── AI Models
```

Do not force this exact structure.

Derive the recommendation from the actual app.

---

# 50. Determine what should be deleted

Create a dedicated removal audit.

Categories:

### Definitely remove

Code with strong evidence that it is:

```text
unused
obsolete
duplicate
unreachable
superseded
```

### Consolidate

Two working implementations that should become one.

### Keep

Looks similar but serves a legitimately different domain purpose.

### Investigate

Not enough evidence to safely remove.

Be conservative.

Deletion is valuable only when safe.

---

# 51. Every deletion recommendation needs evidence

For each proposed deletion include:

```text
file/path
symbol/component
why it is unnecessary
references/search performed
canonical replacement
risk
tests required before deletion
```

Do NOT create tasks such as:

```text
clean unused files
```

without naming them.

---

# 52. Look for obsolete compatibility code

The recent Vision Models implementation may have introduced compatibility between:

```text
model_name
custom filesystem .pt path
model_version_id
```

Determine whether old transitional paths remain necessary.

If internal compatibility layers no longer have a caller, recommend deleting them.

Do NOT break legitimate historical data compatibility.

---

# 53. Review API consistency

Check endpoint naming and semantics across:

```text
agriculture
vision models
video analysis
```

Look for:

```text
different pagination conventions
different status schemas
different error formats
duplicated upload endpoints
inconsistent IDs
```

Recommend convergence where useful.

Avoid API churn purely for aesthetics.

---

# 54. Review frontend module boundaries

Determine whether the recently created Vision Models frontend belongs:

```text
inside agriculture
as a submodule
or as its own top-level domain
```

based on actual coupling and routing.

If duplicate agriculture/vision API clients now exist, identify how they should be organized.

---

# 55. Review backend module boundaries

Determine whether boundaries among:

```text
agriculture
video_analysis
vision_models
```

are healthy.

Good separation might be:

```text
agriculture
    agricultural domain interpretation

vision_models
    datasets/training/model registry

video_analysis
    generic media inference pipeline
```

But do not force this if current architecture differs for good reasons.

Identify circular imports or cross-module leakage.

---

# 56. Identify "god modules"

Flag files/classes that accumulate too many responsibilities.

For each, specify concrete responsibilities currently mixed.

Example:

```text
api.py currently handles:
- routing
- filesystem
- validation
- model training
- aggregation
```

Then propose exact extraction boundaries.

Do not simply recommend "refactor api.py".

---

# 57. Find synchronous blocking work

Search async FastAPI paths for:

```text
OpenCV
filesystem operations
hashing
model inference
image processing
large serialization
```

being run directly on the event loop.

Verify use of existing `run_blocking` infrastructure.

Flag real violations.

---

# 58. Check temporary-file lifecycle

Inspect:

```text
uploaded video
extracted frames
dataset exports
training temp files
SAHI artifacts
evaluation plots
```

Determine whether temporary files are cleaned safely.

Do not delete persistent evidence/models needed by historical records.

---

# 59. Check storage growth

Estimate likely growth from:

```text
videos
training frames
thumbnails
annotations
model weights
training runs
evaluation plots
detection crops
```

Identify unbounded growth.

Recommend lifecycle/retention policies only where clearly needed.

---

# 60. Validate model provenance

For every video analysis result verify the system can determine:

```text
which model
which model version
which checksum
which confidence
which small-object setting
which tracking setting
```

Historical analysis must remain reproducible/understandable.

Flag missing provenance.

---

# 61. Testing quality audit

Inspect whether tests currently verify behavior or merely implementation details.

Find missing tests for critical flows.

Pay particular attention to:

```text
organization isolation
model registry
annotation persistence
evaluation persistence
tracking reset
SAHI coordinate conversion
failed jobs
full frontend workflow
```

Recommend missing tests as roadmap tasks.

---

# 62. Benchmark UX, not visual decoration

Competitor research should focus primarily on:

```text
workflow
information architecture
analysis options
speed to insight
result usefulness
mapping
decision support
```

Do not spend the audit copying branding, colors or marketing-page design.

The goal is a better agriculture product, not visual imitation.

---

# 63. Research current technical best practices

Where relevant, check latest official documentation for current versions of:

```text
Ultralytics
SAHI
Supervision
PyTorch
React
TanStack Query
MUI
React Konva
OpenCV/FFmpeg/PyAV if considering video changes
SQLAlchemy
Celery
```

Use primary/official sources for technical recommendations.

Do not recommend APIs based on outdated examples.

---

# 64. Do not start implementation yet

During this task do NOT perform major production changes.

Allowed changes:

```text
tasks.txt
```

and, only if necessary:

```text
small temporary/local profiling scripts
```

Do not commit temporary analysis artifacts unless they are genuinely reusable.

The primary repository change expected from this task is:

```text
tasks.txt
```

---

# 65. `tasks.txt` must be implementation-ready

Create/update:

```text
tasks.txt
```

Do not write a vague brainstorm.

It must serve as the actual implementation roadmap.

---

# 66. Preserve useful existing tasks

If `tasks.txt` already exists:

read it first.

Do not blindly overwrite valuable unfinished tasks.

Reconcile them with current findings.

Remove completed/outdated tasks only when confident.

Avoid duplicate roadmap entries.

---

# 67. Roadmap priority levels

Organize the roadmap into:

```text
P0 — correctness / data loss / security / broken core flow
P1 — major UX and performance improvements
P2 — architecture simplification / maintainability
P3 — product enhancements inspired by benchmark research
P4 — optional / experimental future work
```

Do not put cosmetic issues in P0.

---

# 68. Roadmap phases

Within priorities, organize work into sensible implementation phases.

Example:

```text
Phase 1 — Critical Bugs
Phase 2 — Simplify Agriculture Flow
Phase 3 — Video Analysis Performance
Phase 4 — Vision Models UX
Phase 5 — Agriculture Insights
Phase 6 — Cleanup and Consolidation
Phase 7 — Advanced Capabilities
```

Derive actual phases from findings.

---

# 69. Required task format

Every task in `tasks.txt` must resemble:

```text
[P1][PERF][VIDEO] Batch video inference instead of one-frame inference

Problem:
Current pipeline performs one model.predict call per frame, causing ...

Evidence:
- backend/modules/video_analysis/service/...
- benchmark: ...
- profiler: ...

Implementation:
1. ...
2. ...
3. ...

Files likely affected:
- ...
- ...

Acceptance criteria:
- ...
- ...
- processing result remains equivalent within ...

Tests:
- ...
- ...

Performance target:
- reduce inference overhead by at least X where benchmark supports such a target

Dependencies:
- none / task IDs

Risk:
- Low / Medium / High
```

Do not invent numeric performance targets without a benchmark.

If no benchmark exists write:

```text
Performance target:
Establish baseline first; optimize based on measured bottleneck.
```

---

# 70. Give every task an ID

Use stable IDs such as:

```text
AGR-001
AGR-002
VID-001
ML-001
PERF-001
CLEAN-001
UX-001
```

so future Codex sessions can be instructed:

```text
Implement VID-003 and PERF-002.
```

---

# 71. Tag tasks

Useful tags:

```text
BUG
SECURITY
PERF
UX
FRONTEND
BACKEND
DATABASE
ML
VIDEO
AGRICULTURE
CLEANUP
TEST
ARCH
```

Multiple tags are allowed.

---

# 72. Explicit deletion section

Create:

```text
REMOVAL / CONSOLIDATION PLAN
```

in `tasks.txt`.

For each item classify:

```text
DELETE
MERGE
KEEP
INVESTIGATE
```

Example:

```text
[CLEAN-004][DELETE]

Target:
frontend/src/...

Reason:
Superseded by ...

Evidence:
No imports except ...
Route removed in ...

Replacement:
...

Required validation:
...
```

---

# 73. Quick wins section

Create a:

```text
QUICK WINS
```

section.

Include only changes that are:

```text
low risk
high confidence
small effort
useful
```

Examples could include:

```text
eliminate duplicate API request
fix query invalidation
lazy-load heavy workspace
add missing index
remove obsolete dropdown
```

based on actual findings.

---

# 74. Performance section

Create a dedicated performance plan.

Split into:

```text
Frontend
API
Database
Video decoding
Inference
SAHI
Tracking
Storage
```

For each list:

```text
current behavior
evidence
expected bottleneck
recommended change
how to measure improvement
```

---

# 75. Product/UX section

Create a:

```text
AGRICULTURE PRODUCT FLOW
```

section in `tasks.txt`.

Document:

### Current flow

Actual existing user path.

### Problems

Concrete friction.

### Recommended flow

A simpler proposed user journey.

### Why

Benchmark/product reasoning.

### Tasks

IDs implementing it.

---

# 76. Competitor insights section

Create:

```text
COMPETITOR INSIGHTS
```

in `tasks.txt`.

For each benchmarked product record only useful ideas.

Example format:

```text
PIX4Dfields
Useful patterns:
- ...
- ...

Do not copy:
- ...

Possible application:
- AGR-xxx
```

Include source URLs/comments if helpful so later implementation sessions can re-check them.

---

# 77. Features to avoid

Create:

```text
NOT RECOMMENDED / NOT NOW
```

Examples could include competitor features that are:

```text
hardware-specific
multispectral-only
too enterprise-specific
not relevant
too complex for current product stage
duplicative
```

Explain why.

This prevents later scope creep.

---

# 78. Architecture section

Document the recommended target boundary between:

```text
Agriculture
Vision Models
Video Analysis
```

using a simple text diagram.

For example only if supported by findings:

```text
Agriculture Domain
        │
        ├── Fields/Missions
        ├── Agriculture insights
        └── Findings
                ▲
                │
Video Analysis ─┘
        ▲
        │
Vision Model Registry
```

Show which existing modules should own which responsibilities.

---

# 79. Identify canonical implementations

Where duplication exists, explicitly state which implementation should survive.

Example:

```text
Canonical frame extraction:
backend/modules/video_analysis/service/frame_extractor.py

Remove/merge:
...
```

Do this for:

```text
video assets
model resolution
frame extraction
telemetry matching
storage
API client
status components
model selectors
```

when applicable.

---

# 80. Create a dependency graph

For roadmap tasks that depend on each other, make ordering explicit.

Example:

```text
VID-003 → PERF-002 → UX-011

ML-004 → AGR-019
```

Avoid a roadmap where every task is independently listed but cannot actually be implemented independently.

---

# 81. Estimate complexity

For each task add:

```text
Size:
S / M / L / XL
```

Use:

```text
S = localized change
M = several files / one coherent feature
L = substantial vertical slice
XL = should probably be decomposed
```

If a task is XL, split it if possible.

---

# 82. Define success metrics

For major improvements define measurable outcomes.

Examples:

```text
Video analysis:
processing time per video minute

Frontend:
requests made entering Agriculture page

Dataset:
time to generate curated frames

Video results:
time to interactive for 10k detections

Storage:
bytes generated per 1k detections
```

Do not make up baseline values.

Mark baseline measurement tasks where needed.

---

# 83. Final roadmap summary

At the very top of `tasks.txt`, include a short executive summary:

```text
Current assessment
Top 5 problems
Top 5 opportunities
Recommended implementation order
```

Keep this concise.

The rest of `tasks.txt` should contain detail.

---

# 84. Final console/report response

After writing `tasks.txt`, report:

### What was inspected

Exact major modules.

### Tests/checks run

Commands and results.

### Competitors researched

Names only plus most relevant lessons.

### Most serious bugs

Top findings.

### Biggest performance bottlenecks

Based on evidence.

### Main simplification opportunities

Especially duplicates/removals.

### `tasks.txt`

Confirm it was created/updated.

### Suggested first implementation batch

Give approximately:

```text
3–6 task IDs
```

that should be implemented next.

Do NOT implement them in this audit session.

---

# 85. Quality bar

The audit is incomplete if it merely says things like:

```text
Improve performance.
Refactor the frontend.
Improve UX.
Add more tests.
Remove duplicate code.
```

Every important recommendation must identify:

```text
what
where
why
evidence
how
acceptance criteria
tests
```

---

# 86. Central objective

The ultimate objective is not simply cleaner code.

The target product should let an agriculture user move naturally through:

```text
FIELD
   ↓
MISSION
   ↓
CAPTURE
   ↓
ANALYZE
   ↓
UNDERSTAND
   ↓
LOCATE PROBLEMS
   ↓
TAKE ACTION
```

while the custom-AI path remains:

```text
DATA
   ↓
LABEL
   ↓
TRAIN
   ↓
EVALUATE
   ↓
DEPLOY
   ↓
ANALYZE
```

The implementation roadmap should make these flows:

```text
simpler
faster
more reliable
more agriculturally useful
easier to maintain
```

and should explicitly remove unnecessary duplication and obsolete implementation where it is safe to do so.

Do the research and analysis first.

Then write the complete implementation roadmap to:

`tasks.txt`

Do not begin the roadmap implementation during this task.
