
• After installing ROS 2, run these checks in order:

  source /opt/ros/jazzy/setup.bash
  gz topic -l | grep -i image
  ros2 topic list

  Then with Gazebo and SITL running, test the irrigation bridge path:

  backend/scripts/smoke_test_irrigation_ros2.sh <mission_id> 20

  If gz topic -l | grep -i image shows nothing, the camera stream is not being exposed yet and that is the first thing to fix. If you want, once ROS 2 is installed, I can help you diagnose that exact step.

