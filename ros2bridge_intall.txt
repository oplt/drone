# install
sudo apt update
sudo apt install -y ros-jazzy-ros-gz ros-jazzy-ros-gz-bridge ros-dev-tools

# create package
cd ~/Desktop/Projects/drone_app
mkdir -p ros2_ws/src
cd ros2_ws/src
ros2 pkg create drone_gz_bridge --build-type ament_python \
  --dependencies ros_gz_bridge ros_gz_sim sensor_msgs nav_msgs geometry_msgs rosgraph_msgs tf2_ros

cd drone_gz_bridge
mkdir -p config launch

# add warehouse_bridge.yaml
# add warehouse_bridge.launch.py
# update setup.py

# build
cd ~/Desktop/Projects/drone_app/ros2_ws
source /opt/ros/jazzy/setup.bash
colcon build --symlink-install
source install/setup.bash

# run bridge
ros2 launch drone_gz_bridge warehouse_bridge.launch.py

# verify
ros2 topic list | grep warehouse
ros2 topic hz /warehouse/mid360/points



odom → iris_with_standoffs/base_link → iris_rplidar_rgbd/mid360_lidar_link/mid360_lidar